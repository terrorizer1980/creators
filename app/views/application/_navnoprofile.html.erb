<% unless defined?(navbarstyle) && navbarstyle != '' %>
	<% 	navbarstyle = 'inverted blue' %>
<% end %>

<div id="mainMenuBar" class="ui <%= navbarstyle %> menu">
	<div class="left menu">
		<%= render "nav_static", :homepath => dashboard_path %>
		<%#= render "nav_creator_central" %>
	</div>

  <div class="right menu">
    <%= render current_user.recruiter && !current_user.profile.nil? ? "nav_user_recruiter" : "nav_user" %>
  </div>
</div>