<%= form_for(@channel_category, html: {class: 'ui form', remote: true}) do |f| %>
  <% if @channel_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@channel_category.errors.count, "error") %> prohibited this channel_category from being saved:</h2>

      <ul>
      <% @channel_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit class: 'ui positive button' %>
  </div>
<% end %>
